# CRATES WORKSPACE GUIDE

**Status (2026-09-19):** this file described the six-crate stub-era layout
(gateway/repo/provider as placeholders, the `excute_command` typo, binary-only
zen-cli) and had drifted far out of date — it actively misled any agent that
read it. It is now a pointer only.

**The authoritative project knowledge base is the root `AGENTS.md`.**

It tracks the real 12-crate workspace, the dependency graph, the type taxonomy,
the command table, the agent/tool inventory, and the per-feature decisions.
Update it (never this file) when architecture changes.
