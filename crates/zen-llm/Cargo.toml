[package]
name = "zen-llm"
version = "0.1.0"
edition.workspace = true
description = "LLM provider connections, models, and transport layer for zenspace"

[dependencies]
zen-core = { path = "../zen-core" }
tokio = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }

# LLM abstraction (Phase 3+)
# rig-core = { version = "0.37", optional = true }

[features]
default = []
# rig = ["dep:rig-core"]