{%- set package_name = modules.entity.full_package -%}
{%- set table_entity = model.feature_name ~ model.class_name ~ "Entity" | to_pascal_case -%}
{%- set table_entity =  modules.entity.full_name  -%}

package {{ package_name }};
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import java.math.BigDecimal;
import java.util.Date;
/**
* {{ model.class_name }}
* {{ model.class_comment }}
*/
@Data
@AllArgsConstructor
@NoArgsConstructor
public class {{ table_entity }} implements java.io.Serializable{

    {% for field in model.fields %}
    /**
    * {{ field.field_name }}
    * {{ field.field_comment }}
    */
    private {{ field.java_type }} {{ field.field_name }};
    {% endfor %}

    {% for field in model.fields %}
    public void set{{ field.field_name | to_pascal_case }}({{ field.java_type }} {{ field.field_name }}) {
        this.{{ field.field_name }} = {{ field.field_name }};
    }
    public {{ field.java_type }} get{{ field.field_name | to_pascal_case }}() {
        return this.{{ field.field_name }};
    }
    {% endfor %}

}
